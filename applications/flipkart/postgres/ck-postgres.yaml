apiVersion: acid.zalan.do/v1
kind: postgresql
metadata:
  name: ck-postgres
  namespace: codekarma
spec:
  teamId: flipkart
  numberOfInstances: 3

  volume:
    size: 10Gi
    storageClass: ckn-gcp-sc

  postgresql:
    version: "15"

  users:
    cknexus:
      - superuser
      - createdb

  databases:
    ckservicedb: cknexus
